# Hosted Server Configuration
# Copy this file to .env and update values

# MongoDB Connection
# Make sure this points to the SAME database as the PLC local server
MONGODB_URI=mongodb://localhost:27017/plc_integration

# Server Port
PORT=5000

# Optional: For production, use Atlas URI
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/plc_integration?retryWrites=true&w=majority
